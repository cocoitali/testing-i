const enhancer = require("./enhancer.js");
//test away!

describe("the enhancer", () => {
  describe("the repair function", () => {
    it("should restore the durability", () => {
        // arrange : set up any mock data we might use
        const longsword = {};

        //act : pass things into the function we want to test
        const result = enhancer.repair(longsword);

        //assert : make any assertions here
        //add code here <---------------
    });
  });
});


//****** hint: you will have to make mock data in order to test these functions correctly */